{{#if view.getYoutubeThumbnail}}
  <div {{bind-attr class=":youtube-thumbnail view.isPlayerVisible:stop"}} {{action toggleVisibility target="view"}} {{bind-attr style="view.getYoutubeThumbnail"}}>
    <div class="youtube-thumbnail-inner"></div>
  </div>
  {{#if view.isPlayerVisible}}
    <div class="youtube-iframe">
      <iframe {{bind-attr src="view.getYoutubeLink"}} width="400" frameborder="0" height="224" allowfullscreen></iframe>
    </div>
  {{/if}}
{{/if}}
